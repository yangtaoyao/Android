<resources>

    <!-- Base application theme. Theme.AppCompat.Light.DarkActionBar -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="android:actionOverflowButtonStyle">@style/weixinActionOverflowButtonStyle</item>-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!---->
        <item name="android:fitsSystemWindows">true</item>
        <!-- To change the popup menu and app text color  -->
        <item name="android:textColor">@android:color/black</item>
        <!-- To change the background of options menu-->
        <item name="android:itemBackground">@android:color/white</item>

        <item name="actionBarSize">50dp</item>
    </style>

    <style name="AppSubTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="android:actionOverflowButtonStyle">@style/weixinActionOverflowButtonStyle</item>-->
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <!--<item name="colorAccent">@color/colorAccent</item>-->

        <!---->
        <item name="android:fitsSystemWindows">true</item>
        <!-- To change the popup menu and app text color  -->
        <item name="android:textColor">@android:color/black</item>
        <!-- To change the background of options menu-->
        <item name="android:itemBackground">@android:color/white</item>

        <item name="actionBarSize">50dp</item>
    </style>



    <style name="CMOptionsMenu" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="android:popupBackground">@android:color/black</item>
        <item name="android:textColor">@android:color/white</item>
    </style>


    <!--全屏主题-->
    <style name="NoActivityFullscreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>



    <!--自定义Dialog-->
    <style name="Pop_anim_style">
        <item name="android:windowEnterAnimation">@anim/pop_in_</item>
        <item name="android:windowExitAnimation">@anim/pop_out_</item>
    </style>

    <style name="Theme_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    
    <!--自定义editView-->
    <style name="my_edittext_style">
        <item name="android:background">@drawable/selector_edittext_bg</item>
    </style>
</resources>
